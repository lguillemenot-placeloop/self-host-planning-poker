version: "3"
services:
  planning-poker:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 80:8000
    volumes:
      - planning-poker-data:/data
volumes:
  planning-poker-data: {}
